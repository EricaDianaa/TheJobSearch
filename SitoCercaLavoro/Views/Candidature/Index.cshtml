@model IEnumerable<SitoCercaLavoro.Models.Candidature>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Annunci.NomeAnnuncio)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Curriculum)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Descrizione)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Stato)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Annunci.NomeAnnuncio)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Curriculum)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Descrizione)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Stato)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.IdCandidatura }) |
            @Html.ActionLink("Details", "Details", new { id=item.IdCandidatura }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.IdCandidatura })
        </td>
    </tr>
}

</table>
